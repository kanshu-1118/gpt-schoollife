export type hobbyType = [
    string,
    string,
]

export type group = {
    name:string;
    icon:string;
    color:string;
    link:string;
    linkbtn:string,
}

export type snsbuttonType = {
    ArrayList:group[]
}