export type hobbyType = [
    string,
    string,
]

export type group = {
    name:string;
    img:string;
    btn:string;
    color:string;
    link:string;
    linkbtn:string,
}

export type snsType = {
    ArrayList:group[]
}